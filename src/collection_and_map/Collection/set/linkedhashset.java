package collection_and_map.Collection.set;

/**
 *
 * linkedhashset 用的还是邻接表，不过在每个节点上不同
 * hashset中的邻接表中的节点 是只有 数据区和下一个数据的指针
 * linkedhashset 的节点是 数据区和下一个数据的指针  上一个插入数据的指针，下一个插入数据指针
 * 所以遍历能按照插入顺序来遍历的，遍历速度也比hashcide快
 * @author cky
 * @create 2020 2 17 12:07
 */
public class linkedhashset {
}
